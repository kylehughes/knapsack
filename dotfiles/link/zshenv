# zsh environment

# Keep mise-managed tools available in non-interactive shells and agent hooks.
if [[ -d "$HOME/.local/share/mise/shims" ]]; then
    export PATH="$HOME/.local/share/mise/shims:$PATH"
fi

# uv
export PATH="$HOME/.local/bin:$PATH"
